abbr -a d docker
abbr -a db docker build
abbr -a de docker exec
abbr -a dl docker logs
abbr -a dlf docker logs -f
abbr -a dps docker ps
abbr -a dr docker run
abbr -a dres docker restart
abbr -a drm docker rm
abbr -a dst docker stop
abbr -a dup docker up

abbr -a dc docker-compose
abbr -a dcb docker-compose build
abbr -a dcdn docker-compose down
abbr -a dce docker-compose exec
abbr -a dcl docker-compose logs
abbr -a dclf docker-compose logs -f
abbr -a dcps docker-compose ps
abbr -a dcr docker-compose run
abbr -a dcres docker-compose restart
abbr -a dcrm docker-compose rm
abbr -a dcst docker-compose stop
abbr -a dcup docker-compose up
